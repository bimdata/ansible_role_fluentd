{{ ansible_managed | comment }}

{% from "macros.j2" import build_conf with context -%}

{% for label in fluentd_labels %}
<label @{{ label.name }}>
{% for key, value in label.items() %}
{% if key != "name" %}
{{ build_conf(key, value, 1) }}
{% endif %}
{% endfor %}
</label>

{% endfor -%}
