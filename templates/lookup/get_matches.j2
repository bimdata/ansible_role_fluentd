{%- from "macros/build_vars.j2" import build_vars with context -%}
[
{%- for varname in lookup('varnames', '^fluentd_.+_match(es)?$', wantlist=True) -%}
  {{ build_vars(lookup('vars', varname)) }}
  {%- if not loop.last %}, {% endif %}
{%- endfor -%}
]
